
function App() {
  return (
    <h1>Hello World || npx create-react-app file_name || cd file_name ||npm run start || npm run build #### npm create vite@latest || cd file_name || npm run dev</h1>
  );
}

export default App;
